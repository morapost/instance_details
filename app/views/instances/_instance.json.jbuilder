json.extract! instance, :id, :isntance_name, :instace_ip, :server_user, :server_password, :app_user, :app_password, :db_name, :db_tns, :db_user, :db_password, :created_at, :updated_at
json.url instance_url(instance, format: :json)
